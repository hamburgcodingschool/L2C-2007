// Example of a simple and funcional function
let res = Math.sqrt(9);
console.log(res);

// Now for our own
function sizeOfText(text) {
    let size = text.length;

    return size;
}

console.log(sizeOfText("Bob"));
console.log(sizeOfText("Johannes"));
console.log(sizeOfText("Barbara"));