// TIMES TABLE

// 1 - create a loop that runs 10 times
// 2 - calculate the result for each iteration of the loop
// 3 - make it pretty

let number = 4;
let counter = 0;

while (counter < 10) {
    counter++;
    // counter = counter + 1;

    let res = number * counter;

    // console.log(counter);  
    // console.log(res);  
    console.log(number + ' X ' + counter + ' = ' + res);
}

